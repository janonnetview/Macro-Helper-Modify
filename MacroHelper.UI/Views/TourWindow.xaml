<Window x:Class="MacroHelper.UI.Views.TourWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="440" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <!-- Sem sombra, aqui e nas outras flutuantes: quem separa a janela do que está atrás é a
         borda do cartão. A sombra do sistema sai no MolduraNativa, chamado no construtor. -->
    <Border CornerRadius="{StaticResource RadiusWindow}"
            Background="{DynamicResource FloatSurfaceBrush}"
            BorderBrush="{DynamicResource FloatBorderBrush}" BorderThickness="1">
        <StackPanel Margin="28,26">
            <Border Width="48" Height="48" CornerRadius="14" Margin="0,0,0,18"
                    Background="{DynamicResource AccentGradientBrush}">
                <TextBlock x:Name="TxtIcone" Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="22"
                           Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock x:Name="TxtTitulo" FontSize="18" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"
                       TextWrapping="Wrap"/>
            <TextBlock x:Name="TxtDescricao" FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"
                       Margin="0,0,0,24"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtPasso" Grid.Column="0" VerticalAlignment="Center"
                           FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                <Button Grid.Column="1" Content="Pular" Style="{StaticResource ButtonSecondary}"
                        Click="BtnPular_Click" Width="80" Height="36" Margin="0,0,10,0"/>
                <Button Grid.Column="2" x:Name="BtnProximo" Content="Próximo"
                        Style="{StaticResource ButtonPrimary}"
                        Click="BtnProximo_Click" Width="100" Height="36"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
